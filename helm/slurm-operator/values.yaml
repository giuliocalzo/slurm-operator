---

# -- Overrides the name of the release.
nameOverride: ""

# -- Overrides the full name of the release.
fullnameOverride: ""

# -- Overrides the namespace of the release.
namespaceOverride: ""

# -- Sets the image pull secrets.
# Ref: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
  # - name: regcred

# -- Set the default image pull policy.
imagePullPolicy: IfNotPresent

# -- Set the priority class to use.
# Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#priorityclass
priorityClassName: ""

# -- Configure Custom Resource Definitions (CRDs).
crds:
  # -- Whether this helm chart should manage the CRD and its upgrades.
  enabled: false

# Operator configurations.
operator:
  # -- Enables the operator.
  enabled: true
  # -- Set the number of replicas to deploy.
  replicas: 1
  # -- Set the image pull policy.
  imagePullPolicy: IfNotPresent
  # -- The image to use.
  # Ref: https://kubernetes.io/docs/concepts/containers/images/#image-names
  image:
    repository: ghcr.io/slinkyproject/slurm-operator
    tag: null
    digest: null
  # Service account configurations.
  serviceAccount:
    # -- Allows chart to create the service account.
    create: true
    # -- Set the service account to use (and create).
    name: ""
  # -- Node label selector for pod assignment.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector
  nodeSelector: {}
    # kubernetes.io/os: linux
  # -- Affinity for pod assignment.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#affinity-and-anti-affinity
  affinity: {}
  # -- Tolerations for pod assignment.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/
  tolerations: []
    # - key: key1
    #   operator: Exists
    #   effect: NoSchedule
  # -- Topology spread constraints for pod assignment. Prefer scheduling replicas
  # across failure domains (nodes, zones, ...) when running in HA.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/topology-spread-constraints/
  topologySpreadConstraints: []
    # - maxSkew: 1
    #   topologyKey: topology.kubernetes.io/zone
    #   whenUnsatisfiable: ScheduleAnyway
    #   labelSelector:
    #     matchLabels:
    #       app.kubernetes.io/name: slurm-operator
  # -- Pod-level security context for the operator pod. Applied to all containers in the pod.
  # Ref: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/
  podSecurityContext: {}
    # runAsNonRoot: true
    # seccompProfile:
      # type: RuntimeDefault
  # -- The container resource limits and requests.
  # Ref: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-requests-and-limits-of-pod-and-container
  resources: {}
    # requests:
    #   cpu: 1
    #   memory: 1Gi
  # -- Container-level security context for the operator container.
  # Ref: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/#set-the-security-context-for-a-container
  securityContext: {}
    # allowPrivilegeEscalation: false
    # capabilities:
      # drop: ["ALL"]
  # PodDisruptionBudget for the operator Deployment.
  # Ref: https://kubernetes.io/docs/tasks/run-application/configure-pdb/
  pdb:
    # -- Enable PodDisruptionBudget.
    enabled: false
    # -- Minimum pods that must remain available after eviction (int or quoted percent).
    minAvailable: 1
    # -- Maximum pods that may be unavailable (int or quoted percent). Rendered only when set, and takes precedence over `minAvailable`.
    maxUnavailable: null
  # -- Set the max concurrent workers for the Accounting controller.
  accountingWorkers: 4
  # -- Set the max concurrent workers for the Controller controller.
  controllerWorkers: 4
  # -- Set the max concurrent workers for the LoginSet controller.
  loginsetWorkers: 4
  # -- Set the max concurrent workers for the NodeSet controller.
  nodesetWorkers: 4
  # -- Set the max concurrent workers for the Restapi controller.
  restapiWorkers: 4
  # -- Set the max concurrent workers for the Token controller.
  tokenWorkers: 4
  # -- Set the max concurrent workers for the SlurmClient controller.
  slurmclientWorkers: 2
  # -- Set the initial concurrency for batched NodeSet sync operations. Each
  # successful batch doubles until the work is exhausted, so raising this
  # reduces the number of sequential barriers on large NodeSets at the cost of
  # weaker error batching. Values below 1 are treated as 1.
  slowStartInitialBatchSize: 1
  # -- Set the log level by string (e.g. error, info, debug) or number (e.g. 1..5).
  logLevel: info
  # -- Set the port used for health checks.
  healthPort: 8081
  # -- Set the port used by the metrics server. Value of "0" will disable it.
  metricsPort: 8080
  # -- Serve the metrics endpoint securely via HTTPS with authn/authz.
  # Requires metricsPort to be non-zero. Scraping clients must present a
  # token authorized to access /metrics (e.g. bound to the metrics-reader
  # ClusterRole). The endpoint uses a generated self-signed certificate, so
  # scrapers must skip TLS verification (e.g. insecureSkipVerify).
  metricsSecure: false
  # -- Enable Go profiling for slurm-operator
  profile: false
  # -- Set the port used for exposing Go profiling metrics.
  # This should never be exposed on a public network.
  profileAddr: localhost:6060
  # -- Enable leader election for slurm-operator
  leaderElection: true
  # -- Comma-separated list of namespaces the operator will watch.
  # If empty, all namespaces are watched.
  namespaces: ""

# Webhook configurations.
webhook:
  # -- Enable the webhook.
  enabled: true
  # -- Enable the pods/binding webhook.
  podsBinding: false
  # -- Set the number of replicas to deploy.
  replicas: 1
  # -- Set the image pull policy.
  imagePullPolicy: IfNotPresent
  # -- The image to use.
  # Ref: https://kubernetes.io/docs/concepts/containers/images/#image-names
  image:
    repository: ghcr.io/slinkyproject/slurm-operator-webhook
    tag: null
    digest: null
  # Service account configurations.
  serviceAccount:
    # -- Allows chart to create the service account.
    create: true
    # -- Set the service account to use (and create).
    name: ""
  # -- Set the timeout period for calls.
  timeoutSeconds: 10
  # -- Node label selector for pod assignment.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector
  nodeSelector: {}
    # kubernetes.io/os: linux
  # Validating webhook configuration.
  validating:
    # -- Action taken when the validating admission webhook is unreachable or returns an error.
    # Ref: https://kubernetes.io/docs/reference/access-authn-authz/extensible-admission-controllers/#failure-policy
    failurePolicy: Fail
    # -- List of MatchConditions, which represents a condition which must by fulfilled for a request to be sent to a webhook.
    # Ref: https://kubernetes.io/docs/reference/kubernetes-api/definitions/match-condition-v1-admissionregistration/
    matchConditions: []
      # expression: CEL expression
      # name: Identifier
    # -- How the rules listed in the validating webhook are matched against incoming requests.
    # Ref: https://kubernetes.io/docs/reference/access-authn-authz/extensible-admission-controllers/#matching-requests-matchpolicy
    matchPolicy: Equivalent
    # -- Full override for the validating webhooks' namespaceSelector, rendered verbatim
    # when set. Replaces `webhook.namespaces` and the default kube-system/kube-node-lease
    # exclusions for these webhooks.
    namespaceSelector: {}
  # Mutating webhook configuration.
  mutating:
    # -- Action taken when the mutating admission webhook is unreachable or returns an error.
    # Ref: https://kubernetes.io/docs/reference/access-authn-authz/extensible-admission-controllers/#failure-policy
    failurePolicy: Ignore
    # -- List of MatchConditions, which represents a condition which must by fulfilled for a request to be sent to a webhook.
    # Ref: https://kubernetes.io/docs/reference/kubernetes-api/definitions/match-condition-v1-admissionregistration/
    matchConditions: []
      # expression: CEL expression
      # name: Identifier
    # -- How the rules listed in the mutating webhook are matched against incoming requests.
    # Ref: https://kubernetes.io/docs/reference/access-authn-authz/extensible-admission-controllers/#matching-requests-matchpolicy
    matchPolicy: Equivalent
    # NOTE: the pods/binding webhook has no namespaceSelector override; scope it via
    # `webhook.namespaces`, which also scopes the controller cache so the webhook can
    # read the pods it intercepts.
  # -- Affinity for pod assignment.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#affinity-and-anti-affinity
  affinity: {}
  # -- Tolerations for pod assignment.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/
  tolerations: []
    # - key: key1
    #   operator: Exists
    #   effect: NoSchedule
  # -- Topology spread constraints for pod assignment. Prefer scheduling replicas
  # across failure domains (nodes, zones, ...) when running in HA.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/topology-spread-constraints/
  topologySpreadConstraints: []
    # - maxSkew: 1
    #   topologyKey: topology.kubernetes.io/zone
    #   whenUnsatisfiable: ScheduleAnyway
    #   labelSelector:
    #     matchLabels:
    #       app.kubernetes.io/name: slurm-operator-webhook
  # -- Pod-level security context for the webhook pod. Applied to all containers in the pod.
  # Ref: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/
  podSecurityContext: {}
    # runAsNonRoot: true
    # seccompProfile:
      # type: RuntimeDefault
  # -- The container resource limits and requests.
  # Ref: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-requests-and-limits-of-pod-and-container
  resources: {}
    # requests:
    #   cpu: 1
    #   memory: 1Gi
  # -- Container-level security context for the webhook container.
  # Ref: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/#set-the-security-context-for-a-container
  securityContext: {}
    # allowPrivilegeEscalation: false
    # capabilities:
      # drop: ["ALL"]
  # PodDisruptionBudget for the webhook Deployment.
  # Ref: https://kubernetes.io/docs/tasks/run-application/configure-pdb/
  pdb:
    # -- Enable PodDisruptionBudget.
    enabled: false
    # -- Minimum pods that must remain available after eviction (int or quoted percent).
    minAvailable: 1
    # -- Maximum pods that may be unavailable (int or quoted percent). Rendered only when set, and takes precedence over `minAvailable`.
    maxUnavailable: null
  # -- Set the log level by string (e.g. error, info, debug) or number (e.g. 1..5).
  logLevel: info
  # -- Set the port used for the webhook server
  serverPort: 9443
  # -- Set the port used for health checks.
  healthPort: 8081
  # -- Set the port used by the metrics server. Value of "0" will disable it.
  metricsPort: 0
  # -- Serve the metrics endpoint securely via HTTPS with authn/authz.
  # Requires metricsPort to be non-zero. Scraping clients must present a
  # token authorized to access /metrics (e.g. bound to the metrics-reader
  # ClusterRole). The endpoint uses a generated self-signed certificate, so
  # scrapers must skip TLS verification (e.g. insecureSkipVerify).
  metricsSecure: false
  # -- Enable leader election for slurm-operator-webhook
  leaderElection: true
  # -- Comma-separated list of namespaces the webhook will watch.
  # If empty, all namespaces are watched.
  namespaces: ""
  # -- Extra annotations on the ValidatingWebhookConfiguration. Merged
  # with chart-managed annotations; user keys win on collision.
  validatingAnnotations: {}
  # -- Extra annotations on the MutatingWebhookConfiguration. Merged
  # with chart-managed annotations; user keys win on collision.
  mutatingAnnotations: {}

#
# Cert-Manager certificate configurations.
certManager:
  # -- Enable cert-manager for certificate management.
  enabled: true
  # -- The secret to be (created and) mounted.
  secretName: slurm-operator-webhook-ca
  # -- Duration of certificate life.
  duration: 43800h0m0s # 5 year
  # -- Certificate renewal time. Should be before the expiration.
  renewBefore: 8760h0m0s # 1 year

#
# External webhook certificate configurations.
externalCertInjection:
  # -- Mount a pre-existing TLS Secret instead of provisioning one.
  enabled: false
  # -- Name of a pre-existing kubernetes.io/tls Secret containing
  # `tls.crt`, `tls.key`, and `ca.crt`. Must be set explicitly when
  # `enabled` is true; intentionally has no default to avoid silently
  # reusing the chart-managed Secret name during migrations.
  secretName: ""

# -- List of Kubernetes Node Conditions, by type, to propagate to the Slurm node drain reason.
# Ref: https://kubernetes.io/docs/reference/node/node-status/#condition
propagatedNodeConditions: []
  # - KubeletUnhealthy

# -- Extra Kubernetes objects to deploy alongside the chart.
# Each entry is rendered as a standalone Kubernetes object.
# Supports Helm templating (e.g. {{ .Release.Namespace }}).
extraObjects: []
  # - apiVersion: v1
  #   kind: ConfigMap
  #   metadata:
  #     name: extra-config
  #   data:
  #     key: value
